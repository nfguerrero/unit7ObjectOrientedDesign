1. because it's an instance variable

2. because the size instance variable is used to restrict the scope of the array when viewing it

3. it would work fine, but then you would have to implement all of the methods for the different boards, rather than
    making it once, and using for all of them